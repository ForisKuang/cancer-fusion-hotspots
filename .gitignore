__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg
.DS_Store

# Local secrets (e.g. OncoKB API token) -- never commit
.env
.env.*

# cohort-scan's on-disk batch Genome Nexus/Pfam-description cache -- a
# reusable cross-run HTTP-response cache, not a reviewable run artifact
runs/.cohort_scan_cache/
